# Copy to .env and fill values for local development / CI

# Postgres settings
POSTGRES_USER=fastapi
POSTGRES_PASSWORD=fastapi_pass
POSTGRES_DB=fastapi_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# App settings
# PORT the app listens on inside the container
PORT=8000